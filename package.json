{
  "name": "morfliq",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/prepare-core.mjs && vite build && node scripts/seo.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "check:release": "node scripts/check-release.mjs"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "0.12.15",
    "@ffmpeg/core": "0.12.10"
  },
  "devDependencies": {
    "vite": "^7.1.7",
    "@playwright/test": "^1.55.0"
  }
}
